package utils;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class WaitUtils {
    public static void waitTillVisible(WebDriver driver, WebElement searchBar) {
    }
}
